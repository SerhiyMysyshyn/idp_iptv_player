plugins {
    id("devlight.android.feature")
}

android {
    namespace = "com.serhiimysyshyn.devlightiptvclient.presentation.feature.home"
}

// Domain, styling, platform, navigation, core-ui, Compose, lifecycle and Koin all arrive
// through the `devlight.android.feature` convention — nothing feature-specific to add here.
