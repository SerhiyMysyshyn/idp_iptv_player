package com.serhiimysyshyn.devlightiptvclient.presentation.feature.home.screen.main.model

/** Stable ids for the drawer rows, so the click handler does not switch on list position. */
internal object MainMenuItemId {
    const val PLAYLISTS = 0L
    const val FAVOURITES = 1L
    const val SETTINGS = 2L
}
